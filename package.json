{
  "author": {
    "email": "louw@ouq77.kiwi",
    "name": "Louw Swart"
  },
  "bugs": {
    "url": "https://github.com/ouq77/express-user-agent-blocker/issues"
  },
  "dependencies": {},
  "description": "Express middleware for blocking access based on User Agent",
  "devDependencies": {
    "@types/express": "4.0.35",
    "code": "4.1.0",
    "lab": "13.1.0",
    "mock-req": "0.2.0",
    "tslint": "5.4.3",
    "typescript": "2.3.4"
  },
  "engines": {
    "node": ">=6",
    "npm": ">=3"
  },
  "keywords": [
    "blocker",
    "express",
    "user-agent"
  ],
  "license": "MIT",
  "main": "./dist/index.js",
  "name": "express-user-agent-blocker",
  "repository": {
    "type": "git",
    "url": "git@github.com:ouq77/express-user-agent-blocker.git"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tslint src/**/*",
    "prepare": "npm run lint && npm run build",
    "pretest": "npm run prepare",
    "test": "lab -c -S -v ./dist/test",
    "test:coverage": "npm run prepare && lab -c -S -v --coverage-exclude dist/test -r html -o ./coverage/coverage.html ./dist/test"
  },
  "version": "1.0.4"
}
